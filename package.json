{
  "name": "pakt-sdk-sample",
  "version": "1.0.0",
  "description": "Sample usage of how to integrate Pakt SDK",
  "main": "index.ts",
  "license": "CC0-1.0",
  "scripts": {
    "dev": "npx nodemon",
    "dev:m": "npx ts-node --project tsconfig.json -r tsconfig-paths/register index.ts",
    "build": "rimraf ./build && tsc"
  },
  "dependencies": {
    "concurrently": "^8.2.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "nodemon": "^3.0.1",
    "pakt-sdk": "^0.1.20",
    "rimraf": "^5.0.1",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@types/node": "^20.4.5"
  }
}
